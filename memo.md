# 計画内容
現在発生している問題に対処するための計画を立てる

## 具体的な計画
現在のdocsにある計画で終了したものは削除して、統合できるのもは統合して
軽量なAIでも実行できるような具体的なステップごとの計画を作成してmarkdownファイルで保存して

## UARTのデータ送信周期が遅い
UARTの通信速度を考えれば、50Hzでもデータを送信できるはずであるが、今は10Hzも出ない
高速データ送信に対応させてほしい
また、動作が不安定であり、起動時は50hzでもすぐに5hz程度になってしまう
この動作をしているときは赤LEDがゆっくり点滅しており、IDLE状態に見える
また、音声のストリーミング、録音、再生ができない
しかし、センサーデータはapp.pyで確認されている
app.pyをターミナルで実行し、その結果を分析して

## BLEでの音声送信
BLEで音声を転送するのには時間がかかってしまう
また、送られてきた音声が大きなノイズしか来ていない
音声送信の時は、センサーデータなどの送信を止めて、音声を送ることに注力して
BLE音声送信の状態遷移を新たに作成

## BLEでのすべてのセンサーデータの送信
すべてのセンサーデータが送られてきていない

## BLEのデータ送信プロトコルの修正
現在は送ることのできるデータ量が限られている
送信プロトコルのパケットを工夫して、なるべく多くのデータを高周期で送れるように工夫して

## 状態遷移の徹底
各通信状態に入った場合は、その通信のみを維持する
uartのみがDMAによってその状態から遷移するトリガーを持つことができる
または、ファームのプログラムがフラグを立てることもある
基本的にフラグに従って状態遷移が行われ、
フラグを立てることができるトリガーはuart dmaとファームによって追加されたトリガーである
そのため、ファームにフラグが追加されていない場合、状態は待機状態を維持し続ける
app.pyなどで各通信状態に接続するにuartでコマンドが送られた場合、
その通信状態に移行する
通信が停止(IDLE状態に移行)または通信で停止コマンドが送られてきた、uart dmaでコマンドの送信がなければ、通信状態(active状態)を維持し続ける
一度待機状態に入れば、uartからのコマンド、またはファームウェアからのフラグが無い限り、待機状態を維持し続ける
この機構を基板、app両方に反映させて
appには今の遷移状態の表示とログを表示する機能を追加
また、不揮発メモリから基板内部のログを取得する機能も追加
ログはtxtファイルで保存


## 待機状態　IDLE状態
LEDが赤色でゆっくり点滅している
TOFは省電力モード
uartのDMAのみによって制御される
または、書き込まれたファームによってトリガーを動作させることも可能である
(仮にファームウェアで自動的にwifiで通信する用のプログラムを書き込んだ場合、
そのファームウェアがトリガーを立てることによって状態が遷移する)
一定時間(現在は3秒)フラグの更新または変更が行われなかった場合、フラグが待機状態になる
つまり、一つ前のトリガーが立った時からの時刻の計測が必要である

## 各通信状態
通信のトリガーによって通信が確立されたら、その通信を維持し続ける
wifi BLE uartそれぞれの通信状態に入ったら、他の通信は基本的に受け入れない
uartのみがDMAによって割り込むことができる(UART独自のフラグが存在する)
wifi BLEは割り込みはできない

## データ取得状態
通信状態に入った場合、通信が終わるとデータ取得状態に遷移する
データ取得状態から、通信状態に入るまでの待機時間は通信周期によって操作される
通信開始→データ取得へ遷移→通信周期に合わせて待機→通信状態へ遷移
を繰り返す
待機する時間は固定ではなく、前回の通信が終わった時刻(通信状態のreturn値を記録)
から計算して、PC側に届くデータが一定周期になるように計算する
各状態に遷移した時間とreturnが返ってきた時間はログに記録されるので、その値を参考に計算する

## 時刻設定
不揮発メモリには、各通信フラグが立った時間と、状態遷移したreturn値などのログデータを記録する
時刻はPCに接続して同期される(app.pyに同期設定を追加)
リセットコマンドが実施されない限り、ログは蓄積され続ける(ログ保存メモリ領域の最大に達したら、配列のデータをシフトして、オーバーフローしないようにする)


そもそも状態遷移の構造によって管理されている場合、上記の問題は両方解決されるはずである

## 状態遷移
データ送信の遷移状態
センサーデータ取得の遷移状態

## 遷移の構造
### idle
idle状態においては、uartのdmaや各種フラグ、またはwifi BLEからのコマンドを待機している状態である
それ以外の動作はしない

### データ送信
データ送信状態に入ったら、メモリに保存されているデータを送信する

### データ取得
データ取得状態に入ったら、センサーデータを取得する
これはセンサー周期以上のデータ取得を要求する可能性がある
センサー周期はコマンド上で指定できる機構を追加すること

### データ保存領域
センサーデータの保存は構造体を用いて、クラス化し、プロジェクト全体で共有する
データ取得では、そのデータを上書きし、
データ送信では、そのデータを送信する