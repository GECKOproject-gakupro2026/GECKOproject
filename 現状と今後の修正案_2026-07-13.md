# 現状と今後の修正案（2026-07-13）

## 1. 完了した作業（今セッション、コミット済み）

ユーザー報告7項目のうち5項目を修正・実機検証・コミット完了。

| # | 項目 | 状態 | コミット |
|---|---|---|---|
| 1 | PhaseFの書き込みバグ（OTA大イメージ適用失敗） | ✅ 修正・検証済 | `b81448c`, `3c676b5` |
| 2 | appでのストリーミング・録音不可 | ✅ 修正・検証済 | `b81448c` |
| 3 | 低消費モードでもTOFのLEDが起動している | ✅ 修正・検証済 | `fb604f6` |
| 4 | プログラム内容と保存場所、メモリ管理の曖昧さ | ✅ ドキュメント化 | `fb604f6` |
| 5 | UARTコマンドの明示 | ✅ ドキュメント化 | `fb604f6` |
| 6 | LED状態と実行状態の対応付け | ✅ ドキュメント化 | `fb604f6` |
| 7 | プログラムファイルの肥大化 | 🔧 **未着手**（本ドキュメントで方針提示） | - |

追加で、作業中に発見した2件の新規バグも修正・検証・コミット済み：
- IDLE→ACTIVE復帰後にToFが復帰しない（`fb604f6`）
- 一度IDLEに入ると二度と復帰できない（`fb604f6`、最重要）
- 連続ホットOTA適用でジャンプ後HardFault（`3c676b5`）

新規ドキュメント2件を追加済み：
- `UARTコマンド・LED状態リファレンス.md`
- `アーキテクチャ・メモリマップ.md`

現在のNonSecureバージョン: **v18**（`NS_APP_VERSION`, `NonSecure/Core/Src/main.c`）

---

## 2. 残タスク：`telemetry.cpp`（1414行）の肥大化解消

### 現状分析

`Secure/Core/Src/telemetry.cpp` の内訳（概算）：

| 区間 | 行数 | 内容 |
|---|---|---|
| 95–190 | 96 | 音声フレーム送信・UART非同期送信ヘルパ |
| 191–390 | 200 | Wi-Fi/TCP状態・`wifiModuleInit`・`tcpServerInit` |
| 391–650 | 260 | `initAudio`・`initSensors`・`initRadio`（BLE含む）・`init` |
| 651–790 | 140 | `collect`・`sendUart`・`sendBle` |
| 786–915 | 130 | `sendTcp`・`pollTcp`（Wi-Fi TCP受信ループ） |
| 917–1095 | 179 | `handleFrame`（OTAフレーム処理）・`processRxByte`・NSCブリッジ |
| 1097–1220 | 124 | `poll()`（メインループ本体） |
| 1221–1414 | 197 | `CommBridge_*`（NSCゲートウェイの実体）・音声DMAコールバック |

**通信スタック3系統（Wi-Fi/BLE/UART）+ OTAフレーム処理 + MCU情報 + 音声キャプチャ + NSCブリッジが1ファイルに同居**しているのが肥大化の実態。

### 分割案（優先度順）

#### 案A: Wi-Fi/TCPスタックを `comm_wifi.cpp` へ分離（最も安全・効果大）

対象: `wifiPinsInit`, `wifiStatusCb`, `wifiModuleInit`, `tcpServerInit`, `tcpCloseClient`, `Service::sendTcp`, `Service::pollTcp` （計約330行）

**分離の根拠**:
- `Service`のprivateメンバへの依存が**`status_`・`tcpSeq_`・`audioStream_`・`respSeq`程度**と少なく、自己完結度が高い
- Wi-Fi用のfile-scope状態（`wifiNetUp`, `tcpListenFd`, `tcpClientFd`, `nextAcceptTick`, `tcpSendFails`）はほぼこの区間だけで閉じている
- OTAのフレームバイトはSecure内に留まる設計（信頼ルート）に影響を与えない——Wi-Fi分離はこの制約と無関係

**リスク**: 低。`Service`クラスのヘッダ（`telemetry.hpp`）で`sendTcp`/`pollTcp`をpublic化するかfriend宣言が必要になる程度。

#### 案B: BLEスタックを `comm_ble.cpp` へ分離

対象: `bleRawProbe`, `initRadio`のBLE初期化部, `Service::sendBle`, BLE ATコールバック（計約150行）

**リスク**: 中。`initRadio`がWi-Fi初期化とBLE初期化を同じ関数内で行っており、分離には関数分割が必要。

#### 案C: OTAフレーム処理を `ota.cpp` に統合

`handleFrame`のOTA関連コマンド処理（`FW_CHUNK`/`FW_COMPLETE`/`FW_APPLY`/`STATUS_REQ`）は既に`ota::Manager`（`ota.cpp`）に委譲する薄いラッパー。これを`ota.cpp`側に移すことで、OTAとテレメトリの境界がより明確になる。

**リスク**: 低〜中。`sendResponse`（ACK/NACK送信）が`Service`のprivateメンバなので、コールバックか`friend`が必要。

#### 見送り案: NSCブリッジ（`CommBridge_*`）の分離

`Comm_*`ゲートウェイの実体（`Secure/Core/Src/secure_nsc.c`から呼ばれる`CommBridge_*`関数群）は、`telemetry::Service`の内部状態（`nsDriven`, `nsCmdRing`, `nsActivity`等）に直接触るため、`telemetry.cpp`に残すのが自然。無理に分離するとかえって可読性が落ちる。

### 推奨する進め方

1. **案A（Wi-Fi分離）のみ、今回実施**。効果とリスクのバランスが良く、`telemetry.cpp`を1414行→約1080行まで削減できる
2. 案B・Cは、次回Phase F（Secure側デッドコード削除）と合わせて実施するのが効率的（そのタイミングで未使用コードの洗い出しも同時にできるため）
3. **ヘッドレスビルドでの新規`.cpp`追加は、過去に踏んだ罠（`.project`の`<linkedResources>`手動編集 + `-importAll`再インポート）が必要**。この手順を踏まずに`-build`だけ叩くとリンクエラーになる

### 実施する場合の手順

1. `Secure/Core/Src/comm_wifi.cpp` + `Secure/Core/Inc/comm_wifi.hpp` を新規作成
2. 該当関数を機械的に移動（ロジック変更なし）
3. `telemetry.hpp`の`sendTcp`/`pollTcp`をpublicにするか、`comm_wifi.cpp`からServiceの`status_`等にアクセスする専用ゲッターを追加
4. `.project`に新規ソースを`<link>`登録 → `-importAll`で再インポート
5. ヘッドレスビルド0エラー確認
6. SWD書き込み→実機で全回帰（Wi-Fi TCP接続、テレメトリ50Hz、OTA Wi-Fi経由）を確認
7. コミット

---

## 3. その他の残課題（優先度は低いが記録しておくべきもの）

| 項目 | 内容 | 優先度 |
|---|---|---|
| AI推論のNonSecure化（Phase D-2） | ユーザー指示でスキップ中。`Comm_GetAudioBuffer`ゲートウェイは用意済みなので、着手する場合はNonSecure側にAIランタイムを移植するだけで済む | 低（明示的に保留） |
| 音声キャプチャのNonSecure化 | DMA転送が動かず断念済み（Phase D）。現状はSecureに残し`Comm_GetAudioBuffer`で生データのみ渡す設計で安定動作中 | 低（再挑戦は非推奨、現状で十分機能） |
| Secure側デッドコード削除（Phase F） | Phase C/DでNonSecureに移った機能の、Secure側の未使用コード（`MX_I2C1_Init`/`MX_I2C2_Init`等）が残っている | 中（telemetry.cpp分割と同時が効率的） |

---

## 4. 次に着手すべきこと

ユーザー承認が得られ次第、**案A（Wi-Fi/TCPスタックの`comm_wifi.cpp`分離）を実施**する。手順は上記の通り。実施後は必ず実機でWi-Fi TCP経由のテレメトリ・OTA更新の回帰確認を行う（このプロジェクトの文化：「実機で観測して初めてPASS」）。
