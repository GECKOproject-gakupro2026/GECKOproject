# アーキテクチャ・メモリマップ

**何がどこに置かれ、なぜそこにあるのか。** TrustZoneでSecure/NonSecureに分けた結果、同じような名前のファイルが両側にあるので、迷ったらここを見る。

---

## 1. 設計原則（この分割の理由）

| | Secure（Bank1） | NonSecure（Bank2） |
|---|---|---|
| **役割** | 信頼ルート。**壊れたら復旧不能**なもの | アプリケーション層。**壊れてもOTAで直せる**もの |
| **OTA更新** | されない（SWD書き込みのみ） | **される**（OTAの更新対象はここだけ） |
| **中身** | OTA機構、通信スタック3系統（Wi-Fi/BLE/UART）、BootGuard、Stage-0ローダー | センサー読み取り、低消費電力ロジック、LED制御、テレメトリ生成 |

**なぜ通信がSecureなのか**: Wi-Fi/BLE/UARTが誤更新で壊れると、OTA経路そのものが消えて二度と更新できなくなる。OTAの存続に関わるものはすべてSecureに置く（ユーザー指示による絶対制約）。

**なぜセンサーがNonSecureなのか**: センサーの読み取り方や使い方はアプリの都合で頻繁に変わる。ここをOTAで更新できることが本システムの主目的。

---

## 2. フラッシュメモリマップ

### 内蔵フラッシュ（2MB、Bank1/Bank2に二分）

| 領域 | アドレス | サイズ | 中身 |
|---|---|---|---|
| **Bank1（Secure）** | `0x0C000000` | 1016KB | Secureアプリ（通信+OTA+BootGuard） |
| NSC領域 | `0x0C0FE000` | 8KB | NonSecure Callableゲートウェイ（`Comm_*`関数の入口） |
| **Bank2（NonSecure）** | `0x08100000` | 1024KB | **OTA更新対象**のNonSecureアプリ |
| └ ベクタテーブル | `0x08100000` | - | SP初期値 + Reset_Handler |
| └ **NSAP appinfo** | `0x08100400` | 16B | magic="NSAP"(`0x4E534150`) + **version** + 予備 |

**バージョン確認**: `STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -r32 0x08100404 1` で走っているNonSecureのバージョンが読める。

### RAM

| 領域 | アドレス | サイズ |
|---|---|---|
| Secure RAM | `0x30000000` | 256KB |
| Secure SRAM4 | `0x38000000` | 16KB |
| **NonSecure RAM** | `0x20040000` | 512KB |
| NonSecure SRAM4 | `0x28000000` | 16KB |
| NS実行中バージョン | `0x200BFFF0` | 4B（NonSecureが起動時に書き込む） |

### 外部NORフラッシュ（MX25LM51245G、64MB）

| 領域 | オフセット | サイズ | 用途 |
|---|---|---|---|
| FWエリアA（ステージング） | `0x200000` | 2MB | OTAで受信中/受信済みの**新しい**イメージ |
| FWエリアB（バックアップ） | `0x400000` | 2MB | 適用前の**直前に動いていた**イメージ（ロールバック用） |
| └ バックアップメタデータ | `0x400000 + 0x1F0000` | 64KB | `SlotMeta`（magic/size/crc16/version）を2重化して保存 |

---

## 3. ソースファイルの配置

### Secure（`Secure/Core/Src/`）— 通信とOTAの信頼ルート

| ファイル | 行数 | 役割 |
|---|---|---|
| **`telemetry.cpp`** | 1414 | ⚠️ **肥大化中**。通信スタック3系統（Wi-Fi/BLE/UART）+ 音声キャプチャ + MCU情報 + フレーム処理 + `Comm_*`ブリッジが同居。将来`comm_service.cpp`へ通信部を分離予定 |
| `main.c` | 1153 | Stage-0ローダー、GTZC/SAU設定（**Secure/NonSecureの境界定義**）、ペリフェラル初期化 |
| `ota.cpp` | 533 | OTAステージング（NOR書き込み）、Bank2適用、NORバックアップ、ロールバック |
| `secure_nsc.c` | 188 | **NSCゲートウェイ**（NonSecureから呼べる唯一の入口） |
| `boot_guard.cpp` | - | 起動試行カウンタ（TAMP->BKP0R）でロールバック判定 |
| `console.cpp` | - | UART RX（GPDMA1_Ch1循環DMA）とprintf |
| `app_main.cpp` | - | **OTAローダーモード時のみ**動く対話ループ（テスト実行、AI推論など） |
| `ai_app.cpp` + `Core/AI/` | - | AI推論（X-CUBE-AI）。NonSecure移行は保留中 |
| `tests_*.cpp` | - | ハードウェアテストスイート（`t`キーで実行） |

### NonSecure（`NonSecure/Core/Src/`）— アプリケーション層（OTA更新対象）

| ファイル | 行数 | 役割 |
|---|---|---|
| `main.c` | 325 | **メインループ**。ACTIVE/IDLEステートマシン、LED制御、テレメトリ送信 |
| `sensors.c` | 212 | 全センサー（ToF/環境/照度/モーション）の初期化・読み取り・低消費電力制御 |
| `ns_audio.c` | - | 音声のRMS/peak/波形計算（生データはSecureから`Comm_GetAudioBuffer`で貰う） |

### 共有ヘッダ（`Secure_nsclib/`）

| ファイル | 役割 |
|---|---|
| `secure_nsc.h` | NSCゲートウェイの宣言（両側から見える） |
| `comm_dto.h` | `FullStatus_t`（165バイトのPOD構造体）。C++側の`telemetry::FullStatus`とバイト同一を`_Static_assert`で保証 |

---

## 4. NSCゲートウェイ（NonSecure → Secure の唯一の通路）

NonSecureはこの7関数だけを通してSecureを呼べる。ポインタ引数は`cmse_check_address_range`で検証してからSecureローカルにコピーして使う。

| 関数 | 用途 |
|---|---|
| `Comm_Poll()` | Secure通信サービスを1回分駆動（**毎ループ必ず呼ぶ**） |
| `Comm_SendTelemetry(const FullStatus_t*)` | テレメトリをSecureに渡して送信させる |
| `Comm_PollHostCommand(uint8_t*)` | ホストからの受信バイトを1つ取る（**IDLEからのウェイク検知源**） |
| `Comm_SetTelemetryEnabled(uint32_t)` | Secure側のテレメトリ送信を止める/再開する（低消費電力用） |
| `Comm_GetLinkStatus()` | BLE/Wi-Fiの接続状態ビット |
| `Comm_GetAudioBuffer(int16_t*, uint32_t)` | Secureが録っている音声窓をコピーして貰う |
| `Secure_ConfirmBoot()` | 起動成功をBootGuardに伝える（ロールバック回避） |

**OTAフレームのバイトはNonSecureに一切出さない**（Secure内で完結）。これが信頼ルートの保証。

---

## 5. ペリフェラルの割り当て（GTZC設定、`Secure/Core/Src/main.c`）

| ペリフェラル | 属性 | 使う側 | 用途 |
|---|---|---|---|
| USART1 + GPDMA1_Ch1 | **Secure** | Secure | VCPコンソール/テレメトリ |
| SPI2 + EXTI | **Secure** | Secure | Wi-Fi（EMW3080） |
| UART4 | **Secure** | Secure | BLE（STM32WB5MMG） |
| OCTOSPI1/2 | **Secure** | Secure | 外部NOR（OTAステージング） |
| MDF1 + GPDMA1_Ch0 | **Secure** | Secure | 音声キャプチャ（MIC2）※ |
| **I2C1** (PB8/PB9) | **NonSecure** | NonSecure | 環境/照度/モーションセンサー |
| **I2C2** (PH4/PH5) | **NonSecure** | NonSecure | ToF（VL53L5CX） |
| **PH1** | **NonSecure** | NonSecure | ToFのLPn（シャットダウン）ピン |
| **PC13** | **NonSecure** | NonSecure | USERボタン |
| **PH6/PH7** | **NonSecure** | NonSecure | 赤/緑LED |

※ 音声はNonSecure化を試みたがDMAが動かず断念。Secureに残し、`Comm_GetAudioBuffer`ゲートウェイで生データだけNonSecureに渡している。

---

## 6. ビルド構造の重要な注意点

**HALソースは`.project`に1ファイルずつ`<link>`登録されている**（フォルダ丸ごとではない）。新しいHALモジュールを使うには2ステップ必要:

1. `Core/Inc/stm32u5xx_hal_conf.h` で `#define HAL_XXX_MODULE_ENABLED`
2. **`.project`の`<linkedResources>`に`<link>`エントリを手動追加**
3. `stm32cubeidec.exe ... -importAll "<repo>"` で再インポート

1だけだとリンク時に `undefined reference to HAL_XXX_*` になる。

BSP（`Drivers/BSP`）は逆に**フォルダリンク + `.cproject`の`excluding=`除外リスト**方式。BSPソースを足す/外すときは`.cproject`の`sourceEntries`を編集する。

**ヘッドレスビルド**:
```bash
stm32cubeidec.exe --launcher.suppressErrors -nosplash \
  -application org.eclipse.cdt.managedbuilder.core.headlessbuild \
  -data <workspace> -build "B-U585I-IOT02A_Secure/Debug"
```

---

## 7. 関連ドキュメント

- **[UARTコマンド・LED状態リファレンス.md](UARTコマンド・LED状態リファレンス.md)** — コマンド一覧、LED表示、プロトコル仕様
- **[TrustZone リファクタリング計画.md](TrustZone%20リファクタリング計画.md)** — この分割に至った経緯、Phase A〜F、実機で踏んだ罠
- **[OTAテスト手順_2026-07-10.md](OTAテスト手順_2026-07-10.md)** — OTAの検証手順
- **[WiFi_STA接続手順_2026-07-11.md](WiFi_STA接続手順_2026-07-11.md)** — Wi-Fi接続の設定
- **[エッジAI推論システム_要件定義書.md](エッジAI推論システム_要件定義書.md)** — 元の要件（11.2節にフレームプロトコル）
