# B-U585I-IOT02A エッジAI開発 — 状況記録（2026-07-08）

## ✅ 最終結果: 全16テストPASS（17:24実機確認）

```
01 LED / 02 ボタン / 03 ToF VL53L5CX / 04 環境(HTS221,LPS22HH) / 05 モーション(ISM330DHCX,IIS2MDC)
06 照度VEML3235 / 07 EEPROM M24256 / 08 NOR MX25LM51245G / 09 PSRAM APS6408 / 10 内蔵Flash NV
11 SRAMリングバッファ / 12 マイク1 ADF1 / 13 マイク2 MDF1 / 14 BLE STM32WB5MMG / 15 Wi-Fi EMW3080
16 TrustZone/GTZC  → total=16 pass=16 fail=0
```

### 追加で判明・修正した事項（2回目以降の修正）
- **PSRAM**: BSPはメモリマップドモード時しかDLYB(遅延ブロック)を設定しない→インダイレクトR/W前にPhaseSel/4設定を追加で解決
- **マイクDMA+TrustZone**: GPDMAリンクリストのノードにSSEC/DSEC設定（BSP audio.cを修正）＋チャネルにSEC|**PRIV**|SRC_SEC|DEST_SEC設定でMIC2/MDF1のDMA動作OK（PRIVなしだとULEエラー0x04）
- **MIC1/ADF1のDMAのみ全ゼロ**（原因未解明・ADF1はSRDドメイン特有の可能性）→ HAL_MDFポーリング取得にフォールバックしてマイク自体は検証済み。**AIファームの音声ストリーミングはMIC2/MDF1のDMAを使うこと**
- **Wi-Fi EMW3080**: NOTIFYの受動監視では検出不可。**CS(PB12)をLowにするとFLOW(PG15)が即時Highで応答**する（mx_wifi SPIハンドシェイク）ことで生存確認。リセットシーケンス=PF15 Low 100ms→High→1200ms待ち
- **BLE**: 9600baudで"AT\r\n"→"ERROR"応答（AT server FW稼働中の正常反応）
- **実行中LEDインジケータ**: SysTickでLD6/LD7を150msごとに交互トグル（`g_LedBlinkEnable`でON/OFF、test.cpp+it.c）ユーザー要望対応済み


## ✅ ステータスモニタシステム完成（17:51実機確認、テストファームの後継）

- **FW**: 常時テレメトリ送信ファーム（テストは't'キーでオンデマンド実行に変更）
  - VCP(USART1)を**921600baud**に引き上げ、フルステータス133Bフレームを**10Hz**送信（CRCエラー0確認）
  - フレーム形式=要件定義11.2節（`frame_codec.c/h`、CRC-16/CCITT-FALSE）※後のMiddlewares/protocolの種
  - BLE: STM32WB5MMG ATサーバー経由でP2Pサービス(svc=1)起動＋コンパクト20Bフレームを1Hz notify(char fe42)
  - 収集: ボタン/温湿度/気圧/加速度/ジャイロ/磁気/照度/ToF(1Hz)/音声RMS・ピーク・32点波形(MIC2 DMA常時)/BLE・WiFi生存/RAM・heap・Flash使用量(mallinfo+リンカシンボル)
  - 新規: `telemetry.{hpp,cpp}` `app_main.cpp` `frame_codec.{h,c}`。I2C2を400kHzに変更（b_u585i_iot02a_conf.h）
  - **stm32wb_atの罠**: `stm32wb_at_ll_Init`はライブラリから呼ばれない→アプリで明示呼び出し必須
- **PC**: `pc_side/status_monitor/`（tkinter製UI、`python app.py`で起動）
  - `protocol.py`(パーサ・printfログ分離) / `transports.py`(UART=pyserial 921600, BLE=bleak P2P notify, Wi-Fi=TCP) / `app.py`(UI) / README
  - 実機で64フレーム/10.5s・CRC0・全値デコード確認済み。BLEはボードが"P2PSRV1"等で広告→bleakで接続
  - Wi-Fi(TCP)はPC側実装済みだがボード側TCPサーバーは未実装（mx_wifi+NetXDuoが必要、将来対応。ser2netブリッジなら今でも可）
- **USB高速化の判断（ユーザー確認済み・2026-07-08）**: ネイティブUSB CDC-ACM(USBX+ThreadX)は実装コストが大きく、実測1.3KB/s（帯域の1.4%）に対し921600baud VCPで十分と判断。**921600baud VCPを維持し、エッジAI推論フェーズへ進む**。ネイティブUSB CDCは将来必要になれば再検討
- UIパイプライン検証: `python app.py`実機起動＋UartTransport→FrameParser→decode_statusの統合を別スクリプトで検証、81フレーム/8秒・CRC0・ボタン/温度/BLE/WiFi値正常反映を確認済み

## 追加機能（2026-07-09）
- **音声PCMストリーミング**: FWに CMD 0x03（512サンプル/1024Bフレーム）追加。コンソール 'a'=開始/'s'=停止。オーディオコールバックはtelemetry.cppの`g_AudioEvents`に一本化（tests_audio.cppも共用）。実測5.12秒81,920サンプル欠損ゼロ・テレメトリ10Hz同時・CRC0
- **app.pyタブ化**: ダッシュボード/全データ(全項目Treeview一覧)/オーディオ(リアルタイム波形・WAV録音 recordings/・winsound再生)。transportsにwrite()追加（UART/TCPからボードへキー送信可、BLEは受信のみ）
- BLE生存判定をUART4受信バイト有無に緩和（BLE=OK確認済み）。BLE接続はP2PSRV1をbleakでスキャン→fe42購読（コンパクト版1Hz）
- Wi-Fi(TCP)トランスポートはser2net等のブリッジ用。ボード側TCPサーバーは未実装（EMW3080スタック未導入）

## 通信改善ラウンド完了（2026-07-10）

### ✅ Wi-Fi SoftAP化（実機検証済み）
- ボードが自分のWi-Fiを発信: **SSID=U585-IOT02A / pass=u585iot02a / ch6 / IP=192.168.4.1**（app_config.hのCFG_WIFI_MODE_SOFTAP=1、=0で従来のSTA接続）
- PCをこのWi-Fiに接続→アプリの「Wi-Fi (TCP)」で **192.168.4.1:5000** → テレメトリ29.8Hz＋**音声ストリーミング7.42秒欠損ゼロ＋WAV録音成功**（CRC0）
- TCP切断検出はsend失敗2連続で判定（recvのエラーコードはタイムアウトでも-1が返り信用できない）

### ✅ BLE全センサーデータ化（実機検証済み）
- WBモジュールはBLE_AT_Server書換済み（SW4/SW5切替経由）。P2PSRV1としてアドバタイズ、MTU=251
- MiniStatus v2（39B）: ボタン/温湿度/気圧/照度/ToF/加速度/ジャイロ/磁気/音声RMS・ピーク/稼働時間/ダイ温度/BLE・WiFi・ToF状態/CPU負荷 を1通知1Hzで配信
- **BLEでの音声ストリーミングは物理的に不可**: WBモジュールへのATリンクがUART4 9600baud（≒960B/s）で、音声32KB/sの1/30以下。音声はWi-Fi(TCP)/UARTで提供

### ✅ データ取得周期の限界引上げ（実機実測）
- フレームv2(165B)を**UART 50.8Hz実測**（921600baud・CRC0）、Wi-Fi TCP時29.8Hz
- UART送信をIT化(非ブロッキング、uartSendAsync+TxCpltCallback+USART1_IRQHandler)。ステータスは送信中ならドロップ、音声は最大15ms待ち
- センサー別周期: モーション毎フレーム(ODR 208Hz)、環境100ms(HTS221 12.5Hz/LPS22HH 75Hz)、照度200ms、ToF500ms(1回~35msのI2C)、MCU情報500ms
- ボトルネック計測はpoll()内蔵プロファイル（起動後3回だけ[PROF]行を出力）

### ✅ 照度センサー0固定の修正
- 原因: VEML3235のCONFレジスタ**bit15 SD_ALS（ALSチャネル個別シャットダウン）**をドライバがクリアしない
- veml3235.c Pwr_Onにbit15クリアを追加 → 実測 light=126〜895

### ✅ MCU詳細情報（FullStatus v2、165B）
- ダイ温度（内蔵ADC1+TEMPSENSOR、**HAL_PWREx_EnableVddA()が必須**だった）/VDDA電圧/SYSCLK/HCLK/リセット要因(RCC_CSR)/CPU負荷/Flashサイズ(工場値2048KB)/96bit UID/IDCODE
- ADCカーネルクロック=HSI。実測 die=39〜46°C, VDDA=3291mV
- protocol.py・UI全データタブ対応済み（v1後方互換あり）

### 実測サマリー
| リンク | 内容 | 実測 |
|---|---|---|
| UART(VCP 921600) | フルv2 50Hz+音声32KB/s | 50.8Hz/CRC0、音声5.12s欠損0 |
| Wi-Fi TCP(SoftAP) | フルv2+音声 | 29.8Hz、音声7.42s欠損0、WAV保存 |
| BLE(P2P notify) | 全センサー39B | 1Hz、MTU251、CRC0 |

### 次期タスク（OTA: 設計方針のみ確定）
1. プロトコル: 11.2節どおり CMD 0x04=FW_CHUNK(offset4B+data≤1016B)/0x05=FW_COMPLETE(size4B+CRC16)/0x7E ACK/0x7F NACK
2. 受信経路: UART/TCP共通のフレームデコーダをボードに実装（現状は1文字コマンドのみ）
3. ステージング: 外部NOR(MX25LM51245G)のFW領域B(0x200000、11.3節マップ)へ書き込み→CRC検証→ACK
4. 適用: 評価ボードでは「ステージング済みイメージをブートローダで内蔵Flashへコピー」方式を次フェーズで実装（TrustZone構成のためバンクスワップよりステージング+コピーが安全）

## 無線通信の調査・統合結果（2026-07-09）
### BLE
- PC側bleakでスキャン→**P2PSRV1がアドバタイズされていない**
- 原因: STM32WB5MMGモジュールの工場FWはstm32wb_at ATサーバーではない（全ATコマンドにERROR応答）。**BLE_AT_Server FWの書き込みが必要**＝CN5コネクタ＋外付けST-LINKドングル＋SW3切替の物理作業（ST公式手順: FUS+BLEフルスタック+BLE_AT_Server_reference.hexをSTM32CubeWBパッケージから）→ ユーザー作業待ち
- PC側BLEトランスポート/テストスクリプト(scratchpad/ble_test.py)は準備済み。モジュールFW書換後に即テスト可能
- FWのbleRawProbe（起動時にAT/AT+BLE_TEST?/AT+BLE_SVC=1の生応答をログ）で診断可能

### Wi-Fi (mx_wifiドライバ統合・動作確認済み)
- `Drivers/BSP/Components/mx_wifi` 導入、ベアメタル(RTOS無し)+SPI(ポーリング)構成
- `Secure/Core/Inc/mx_wifi_conf.h`: ピン定義(NSS=PB12/RESET=PF15/NOTIFY=PD14/FLOW=PG15)、MXCHIP_SPI=hspi2
- **SPI2はMX設定(4bit/ハードNSS/80Mbps)からWi-Fi用(8bit/ソフトNSS/20Mbps)にwifiPinsInit()で再初期化**（重要な落とし穴だった）
- EXTI14/15(NOTIFY/FLOW立上り)→mxchip_WIFI_ISR。it.cにハンドラ追加
- **実機動作: EMW3080 FW=V2.1.11, MAC=84:9D:C2:96:C8:03 取得成功**
- モジュールFWがドライバ要求(V2.3.4)より古い→mx_wifi.cのMX_ASSERT無限ループを警告+続行にパッチ済み（ベストエフォート動作）。正式にはST「X-WIFI-EMW3080B」パッケージでモジュールFW更新推奨
- AP接続: `app_config.h`の`CFG_WIFI_SSID`/`CFG_WIFI_PASSWORD`に記入すると起動時に自動接続しIP表示（現在は空でスキップ）。TCPサーバー(CFG_WIFI_TCP_PORT=5000)は次段階で実装予定
- 備考: 照度raw=0が続く事象あり（環境光の可能性、要観察）。MIC1/ADF1のDMA問題は未解決のままMIC2運用

## BLE/Wi-Fi双方向通信テスト 進行状況（2026-07-09 午後）

### 方針（ユーザー指示）
- **BLEを先に完了させてからWi-Fiに着手**
- Wi-Fiは最終的にフルTCP双方向通信（AP接続→TCPサーバー→PC送受信）まで実装する
- Wi-Fi AP: SSID=`Buffalo-Wifi-2.4G`（app_config.hに記入して使用。EMW3080は2.4GHz帯のみ）

### BLE調査で確定した事実
1. **公式クライアント（STM32CubeU5 `BLE_AT_Client`）確認済み**: UART4=**9600baud** 8N1（READMEの115200はVCP側）。リセット/ウェイクアップGPIOは一切使わず、起動後 `HAL_Delay(2000)` → `AT` → `AT+BLE_SVC=1` を送るだけの構成
2. **現在のモジュールは9600/115200いずれでも完全無応答**（ブリッジ経由で AT / AT+BLE_TEST? / AT+BLE_RST=1 等を送信テスト済み）。昨日の「ERROR応答」は再現せず → **モジュールにBLE_AT_Server FWが載っていないことが確定**
3. **書き換えはオンボードST-LINKで可能**（mind.beブログ確認: 外付けドングル不要）:
   - 手順: **SW4=OFF, SW5=ON**に切替（ST-LINKのSWDがWBモジュール側に切替わる）→ CubeProgrammerで FUS確認(v1.2.0以上なら更新不要) → BLEフルスタック書込 → BLE_AT_Server_reference.hex書込 → **SW4/SW5を元に戻す**
   - スタック書込アドレス: v1.18.0の `stm32wb5x_BLE_Stack_full_fw.bin` は **0x080CE000**（STM32WB5MMG=1MB品）。AT serverアプリはアドレス指定不要（hexに内包）
4. バイナリ入手状況: `BLE_AT_Server_reference.hex`(131KB)と`stm32wb5x_FUS_fw.bin`(24KB)はGitHub v1.18.0タグから取得済み（scratchpad/wb_fw/）。**`stm32wb5x_BLE_Stack_full_fw.bin`はGit LFSで、raw取得がレート制限に到達し未取得**（git sparse clone + git lfs pullで取得予定、またはst.comのSTM32CubeWBパッケージから）

### 今日のFW/ツール変更点（ビルド・書込済み、テレメトリ10Hz動作確認済み）
- **stm32wb_at_client.c `Wait_ready`にタイムアウト追加（2秒）**: 純正は無限ビジーループで、モジュール無応答時に`Set(BLE_SVC)`でフリーズしていた（今朝のテレメトリ停止の原因）。タイムアウト時は`client_current_cmd=BLE_NONE`にリセット
- **app_main.cppにVCP↔UART4透過ブリッジ追加**: コンソール'b'+ボーレート番号('1'=9600〜'5'=115200)で進入、ESCで脱出。PCから任意のATコマンド実験が再書込なしで可能
- **ブリッジのUSART1オーバーラン対策**: 9600への1バイト転送(≈1ms)中に921600のPCバーストが来るとORE発生→受信不能になる。FW側でOREフラグ掃除＋PC側スクリプト(at_probe.py)は1バイト6ms間隔のペーシング送信
- scratchpadスクリプト: `serial_monitor.py`(フレーム/ログ分離監視) / `ble_scan.py`(bleakスキャン) / `at_probe.py`(ブリッジ経由AT方言プローブ)
- PC環境: bleakインストール済み。BLEアダプタ動作確認済み（周辺3デバイス検出、P2PSRV1広告なし＝モジュール無FWと整合）

### 途中で起きたトラブルと復旧（教訓）
- ブリッジ滞在中にESCが届かずテレメトリ停止と誤認 → CubeProgrammer CLIの`-halt -coreReg`でPC/LR読取→`addr2line`で**bleBridge内で正常ループ中**と特定（デバッガによる現場検証が有効）
- `STM32_Programmer_CLI -g`はアドレス0x8000000から実行開始しTZ構成では復帰不能 → **復旧は`-rst`(ソフトリセット)を使うこと**

### 次のアクション
1. `stm32wb5x_BLE_Stack_full_fw.bin`のLFS実体を取得
2. ユーザーにSW4=OFF/SW5=ON切替を依頼 → CLIでFUS確認→スタック→AT server書込 → スイッチ復帰
3. 9600でAT応答(OK)確認 → `AT+BLE_SVC=1`でP2P広告 → bleakでnotify購読＋GATT write（fe41=write/LED制御, fe42=notify）で双方向確認
4. BLE完了後: mx_wifiソケットAPI(`MX_WIFI_Socket_*`)でTCPサーバー実装（NetXDuo不要）→ PCからTCP双方向テスト

## ✅ Wi-Fi TCP双方向通信テスト成功（2026-07-09 20:25実機確認）

```
=== RESULT: frames=152 crc_err=0 pongs=5 led_acks=5 ===  → BIDIRECTIONAL TEST: PASS
```
- ボード=TCPサーバー(192.168.11.48:5000) ⇔ PC=クライアント
- ボード→PC: フルステータス133Bフレームを10Hz送信（CRCエラー0）
- PC→ボード: 'p'=PONG応答 / 'l'=LED_GREENトグル+ACK応答（双方向確認）
- テストスクリプト: scratchpad/tcp_test.py（`python tcp_test.py 192.168.11.48 5000 15`）

### 実装内容（telemetry.cpp / telemetry.hpp / app_config.h）
- `CFG_WIFI_SSID="Buffalo-Wifi-2.4G"` / パスワード設定済み（app_config.h）
- mx_wifiソケットAPIでTCPサーバー（**NetXDuo不要**、モジュール内蔵TCP/IPスタック使用）: create→bind(port 5000)→listen→accept(1Hzポーリング)→send/recv
- クライアント接続後はRCVTIMEO=10msでrecvポーリング。'p'/'l'コマンド処理
- 10s以上のストール後はテレメトリ周期をリセット（acceptブロック後のバースト送信防止）

### 解決した問題（この順で全部踏んだ）
1. **EMW3080モジュールFWがV2.1.11のまま** → connect系MIPCコマンド非対応。**V2.3.4への更新が必須だった**
   - 更新手順（UM2839 7.8.3.1）: SW2のbootピンを0→`x-wifi-emw3080b/V2.3.4/SPI/EMW3080update_B-U585I-IOT02A-RevC_V2.3.4_SPI.bin`をU585に書き込み→リセット→コンソールで`flash`コマンド（青ボタン相当）→"Programming successful"→SW2復帰
   - アップデータFWは非TrustZone用のため**TZEN=0化が必要だった**（下記事故参照）
2. **SPI 20MHz+ポーリング受信で大きい応答(>~100B)がオーバーラン** → scan応答・GET_IP応答が"Transmit/Receive data timeout"。**SPI_BAUDRATEPRESCALER_16(10MHz)に変更で解決**（ST公式はDMA前提の20MHz。ポーリング構成では10MHzにすること）
3. **DHCPが動かずIP=0.0.0.0** → mx_wifiドライバは`obj->NetSettings.DHCP_IsEnabled=1`を**アプリが明示設定しないと静的IP 0.0.0.0で接続する**。Connect前に設定して解決（IP=192.168.11.48取得成功）
4. `MX_WIFI_Connect`はDHCP完了前に返る → GetIPAddressを500ms間隔でポーリング（最大10s）
5. `MX_WIFI_Socket_accept`は**未接続時に約10秒ブロック**（MX_WIFI_CMD_TIMEOUT）。select未実装スタブ・非ブロッキング化不可。1Hzでaccept試行し、ブロックは許容（クライアント接続中は発生しない）。既知の制限として記録

### ⚠️ TrustZone regressionでのロックアウト事故と復旧手順（重要な教訓）
- TZEN=0にするため`-ob RDP=0xBB`を実行→**TZEN=1+RDP Level1の組合せでSWD完全ロック**（OB操作・メモリリード全部拒否、`-tzenreg`もSWDでは"not supported"）
- **復旧方法: SW1(BOOT0スイッチ)を1にして黒リセット→RSSブート→`-ob RDP=0xAA TZEN=0`が通る**（フラッシュ全消去を伴う）→SW1を0に戻す
- 復旧後にオプションバイト再設定: `TZEN=1 SECBOOTADD0=0x180000 SECWM1_PSTRT=0x0 SECWM1_PEND=0x7F SECWM2_PSTRT=0x7F SECWM2_PEND=0x0`
  - 注意: TZEN=1書込と同時にSECWM2を指定すると値が正規化されて壊れる場合がある→**SECWM2は別コマンドで再設定して確認すること**
- 今後TZEN⇄非TZの往復が必要な場合はこの手順で安全に実施可能（所要約5分）

### 動作確認済みイベント/ログ（正常時の起動シーケンス）
```
[TLM] EMW3080 FW=V2.3.4 MAC=84:9D:C2:96:C8:03
[TLM] joining AP "Buffalo-Wifi-2.4G"...
[TLM] wifi event: cate=1 STA_UP
[TLM] Wi-Fi connected, IP=192.168.11.48
[TLM] TCP server listening on port 5000
[TLM] TCP client connected from 192.168.11.x (accept took N ms)
```
- 診断用に追加: `MX_WIFI_RegisterStatusCallback`でSTA_UP/STA_GOT_IP等を印字、起動時パッシブスキャンで周辺AP一覧印字（Buffalo-Wifi-2.4G ch1 RSSI-50確認）

### BLE側の残タスク（変更なし・ユーザー物理作業待ち）
- STM32WB5MMGへBLE_AT_Server FW書込（SW4=OFF/SW5=ON→オンボードST-LINKで書込→復帰）
- 必要バイナリ: BLE_AT_Server_reference.hex/FUS取得済み、BLEスタック本体はLFS未取得（git sparse clone予定）

## ✅ BLEモジュールFW書き込み成功（2026-07-09 20:50頃）

ユーザーが**SW4=OFF/SW5=ON**に切替→オンボードST-LINKのSWDがSTM32WB5MMGに切替（Device ID 0x495=STM32WB5xとして認識確認）。

### 実施手順と結果
1. バイナリ取得: `git clone --sparse` + `git lfs pull`でSTM32CubeWB v1.18.0の`Projects/STM32WB_Copro_Wireless_Binaries/STM32WB5x/`一式（scratchpad/wbrepo/）。stack_full=151,732B
2. `-startfus`でFUS起動（FUS_STATE_IDLE確認）
3. **書込アドレスの罠**: 公式表の0x080CE000だと画像末尾(0x080F30B4)が現SFSA=0xF0保護域に食い込み消去失敗（"failed to erase memory"）。`-fwdelete`もIMG_NOT_FOUNDで不可
4. **解決**: FUS 1.1+は任意の空きアドレスからインストール可能→ `-fwupgrade stm32wb5x_BLE_Stack_full_fw.bin 0x080A0000 firstinstall=1` → **Firmware Upgrade Success**。FUSが自動再配置しSFSA=0xCE(0x080CE000)/SBRV=0x33800(CPU2ブート=0x080CE000)に更新
5. `BLE_AT_Server_reference.hex`をCPU1に書込・verify成功、`-startwirelessstack`実行
6. SW4=ON/SW5=OFFに復帰（ユーザー）

### 残テスト（次のアクション）
- U585リセット→bleRawProbe(9600)でAT→OK応答確認
- AT+BLE_SVC=1でP2PSRV1広告→PC bleakスキャンで検出
- fe42 notify購読（MiniStatus 1Hz、ボード→PC）＋fe41へのGATT write（PC→ボード、`stm32wb_at_BLE_EVT_WRITE_cb`でLED制御）で双方向確認

## 🔧 BLE双方向テスト・デバッグ記録（2026-07-09深夜〜07-10未明）

### 発生した問題と原因究明の経緯
1. **SW4/SW5をデフォルト（SW4=ON/SW5=OFF）に戻した直後、ATが完全無応答に**
   - 切り分け実験: ブリッジ経由でAT送信中にモジュールのUART受信ISRフラグを監視 → 送受信とも無反応
   - bleakスキャンでは**P2PSRV1広告は継続**（advertisingは生きている）→ BLEスタック自体はCPU2で正常動作、CPU1アプリだけが死んでいると判明
   - SWDでCPU1(WB5x)のPCレジスタを直接読み取り→**HardFault_Handlerで停止**していることを確認（`stm32wbxx_it.c:99`）
2. **根本原因**: ST公式`BLE_AT_Server`v1.18.0のソースコードのバグ。`Adv_Mgr()`が60秒タイマーで`CFG_TASK_ADV_UPDATE_ID`タスクをセットするが、**このタスクがどこにも`UTIL_SEQ_RegTask`登録されていない**→シーケンサが未初期化の関数ポインタ（NULL相当）を呼び出しHardFault。**起動から60秒前後で必ず発生**（実機でも「90秒後にAT→OK確認→その後何もしないと沈黙」というタイミングで再現一致）
3. **修正**: STM32CubeWB公式リポジトリからソース一式を取得しローカルでビルド（`C:\Users\takut\wb\`に配置、BLE_AT_Server自前ビルド）。`app_ble.c`に`Adv_Update()`（何もしない）を追加して`CFG_TASK_ADV_UPDATE_ID`に登録 → 60秒境界を越えてもAT応答が継続することを確認
4. **副次的な発見と修正**:
   - 切断後に広告を再開しない実装だった → `HCI_DISCONNECTION_COMPLETE_EVT_CODE`ハンドラに`Adv_Request(APP_BLE_FAST_ADV)`を追加
   - AT通信バッファが64バイトで、20バイトnotifyペイロード（0x表記で40文字+ヘッダ）を送ると65文字を超えてオーバーフローする → U585側`stm32wb_at_client.c`の`client_buff_tx`とWB側`at_buffer`を160バイトに拡張
   - **notify自体が届かない問題**: ST公式のP2Pサーバー実装は「ボタン通知専用」に作られており、GATT特性長が固定2バイト、かつ`Manage_Update_Charac()`が`val_tab[0]==1`の時だけ2バイト固定パケットを送る決め打ち実装だった。テレメトリの20バイト可変長フレームを送るため、`p2p_stm.c`の特性長を2→20に変更し、生ペイロードをそのまま送信する`P2PS_STM_Notify_Raw()`を追加、`Manage_Update_Charac()`から呼ぶよう修正（**ビルド・書込・実機検証は次回**）

### ビルド環境（自前ビルドしたBLE_AT_Serverの位置）
- ソース: `C:\Users\takut\wb\Projects\P-NUCLEO-WB55.Nucleo\Applications\BLE\BLE_AT_Server\STM32CubeIDE\`（STM32CubeWB本体のGit LFSクローンから必要フォルダを`git archive`で展開、HAL/CMSISはgitサブモジュールとしてinit）
- ヘッドレスビルド: 別ワークスペース`scratchpad/hws2`を使用（U585用`hws`と分離）
- 出力: `C:\Users\takut\wb\Projects\P-NUCLEO-WB55.Nucleo\Applications\BLE\BLE_AT_Server\STM32CubeIDE\Debug\BLE_AT_Server.elf`
- 書込は必ずSW4=OFF/SW5=ON（WB5xにSWD切替）の状態で`STM32_Programmer_CLI -c port=SWD mode=UR -d <elf> -v -rst`

### 教訓
- ST公式リファレンスアプリでも普通にバグがある（60秒タイマーのNULL呼び出し）。無応答＝自分のミスと決めつけず、SWDでの直接デバッグ（PC/LRレジスタ読み取り→addr2line）が最短の切り分け手段

## ✅ BLE双方向通信テスト成功（2026-07-10 01:25実機確認）

```
=== RESULT: mini_frames=26 raw_notifies=26 crc_err=0 writes=8 ===
BLE BIDIRECTIONAL TEST: PASS
```

- **ボード→PC（notify）**: fe42購読で温度・湿度・ToF距離を含むMiniStatusフレームを1Hzで26フレーム受信、CRCエラー0
- **PC→ボード（write）**: fe41へのGATT writeでLED制御コマンドを8回送信、UARTログで`[TLM] BLE write: svc=1 char=2 len=2 val0=0x00/0x01`を全件確認、実機LED_GREENの点灯/消灯も確認
- テストスクリプト: `scratchpad/ble_bidir_test.py`（`python ble_bidir_test.py 25`）

### 最終的な修正内容（`C:\Users\takut\wb\`のBLE_AT_Server自前ビルド、WB5xモジュールへ書込）
1. `app_ble.c`: `Adv_Update()`ハンドラ追加＋`CFG_TASK_ADV_UPDATE_ID`登録（60秒HardFaultバグ修正）
2. `app_ble.c`: 切断イベントで`Adv_Request(APP_BLE_FAST_ADV)`を呼び広告再開
3. `p2p_stm.c`/`p2p_stm.h`: notify特性長を2→20バイトに拡張、`P2PS_STM_Notify_Raw()`を追加
4. `app_ble.c`の`Manage_Update_Charac()`: ボタン専用の決め打ち送信ロジックを`P2PS_STM_Notify_Raw()`呼び出しに置換（任意ペイロードを送信可能に）
5. U585側（本プロジェクト）: `stm32wb_at_client.c`の`client_buff_tx`と`telemetry.cpp`の`bleAtBuffer`を64→160バイトに拡張（20B notifyペイロードで65文字を超えオーバーフローしていたため）

### BLE/WiFi 双方向通信テスト 総括
これでBLE・WiFiともに双方向通信の実機確認が完了。当初の要件「双方向通信ができることを確認する」を達成。
- BLE PeripheralのGATT特性は用途（ボタン通知 vs 汎用データ）に応じて長さ・送信ロジックがハードコードされていることが多く、リファレンスアプリをそのまま別用途に転用する場合は送信経路の実装まで確認が必要

## 完了した作業

### 1. 環境確認・ツールパス（全て動作確認済み）
| ツール | パス |
|---|---|
| STM32CubeIDE 2.1.1（ヘッドレスCLI） | `d:/App/ST/STM32CubeIDE_2.1.1/STM32CubeIDE/stm32cubeidec.exe` |
| STM32_Programmer_CLI（CubeIDE同梱） | `d:/App/ST/STM32CubeIDE_2.1.1/STM32CubeIDE/plugins/com.st.stm32cube.ide.mcu.externaltools.cubeprogrammer.win32_2.2.400.202601091506/tools/bin/STM32_Programmer_CLI.exe` |
| GCC 14.3（同梱） | `.../plugins/com.st.stm32cube.ide.mcu.externaltools.gnu-tools-for-stm32.14.3.rel1.win32_1.0.100.202602081740/tools/bin/` |
| STM32CubeMX 6.18 | `d:/App/STM32CubeMX/STM32CubeMX.exe` |
| FW_U5 V1.9.0 リポジトリ | `C:/Users/takut/STM32Cube/Repository/STM32Cube_FW_U5_V1.9.0`（BSP/例含む） |
| Python 3.11 + pyserial 3.5 | `python` |
| ボード | COM9（ST-LINK V3E）で接続確認済み |
| ※独立版CubeProgrammerは壊れている | binにjreのみ。CubeIDE同梱版を使うこと |

### 2. オプションバイト（実機反映済み）
- TZEN=1、SECWM1=Bank1全Secure、SECWM2=Bank2全NonSecure、RDP=0xAA、SECBOOTADD0=0x0C000000
- 配置: Secure=Bank1 0x0C000000(1MB)+SRAM1/2(0x30000000)、NS=Bank2 0x08100000(1MB)+SRAM3(0x20040000)、NSC=0x0C0FE000(8KB)

### 3. ビルド・書き込み・監視ワークフロー（確立済み）
```bash
HWS="<scratchpad>/hws"  # ヘッドレス用ワークスペース（GUIと共存可）
stm32cubeidec.exe --launcher.suppressErrors -nosplash \
  -application org.eclipse.cdt.managedbuilder.core.headlessbuild -data "$HWS" \
  -build "B-U585I-IOT02A_Secure/Debug"
# 初回のみ -import 'D:\...\Secure' -import 'D:\...\NonSecure'（Windows形式パス必須）
STM32_Programmer_CLI -c port=SWD mode=UR -d Secure.elf -v   # 書き込み
STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -rst          # リセット
python serial_monitor.py COM9 60                            # ターミナル監視
```

### 4. プロジェクト変更点
- C++対応化: `Secure/.project`にccnature、`.cproject`Debug構成にG++設定（-mcmse/-D/-I/リンカスクリプト）
- BSP導入: `Drivers/BSP/B-U585I-IOT02A/` + `Components/{Common,hts221,iis2mdc,ism330dhcx,lps22hh,veml6030,veml3235,vl53l5cx,m24256,mx25lm51245g,aps6408,stm32wb_at}`（FW_U5 1.9.0からコピー）
- 設定ヘッダ: `b_u585i_iot02a_conf.h` / `mx25lm51245g_conf.h` / `aps6408_conf.h`（Secure/Core/Inc）
- テストファーム: `app_config.h`(設定) / `console.{h,cpp}`(VCP printf/入力) / `test.{hpp,cpp}`(フレームワーク+メニュー) / `tests_board|sensors|memory|audio|wireless|security.cpp`
- main.c は `App_TestMain()` 呼び出しのみ（最小化）
- it.c に GPDMA1_Channel0/6 ハンドラ（mdf[0]→CH6、mdf[1]→CH0）

### 5. 実機テスト結果（1回目: 9/16 PASS）と原因分析
| # | テスト | 結果 | 原因（FAIL分） |
|---|---|---|---|
| 01 | LED | PASS | |
| 02 | ボタン | PASS | |
| 03 | 環境センサー | PASS | %f表示が空（修正3） |
| 04 | モーション | PASS | |
| 05 | 照度 VEML3235 | PASS | |
| 06 | ToF VL53L5CX | FAIL | BSPのi2cリカバリがI2C2ピンをGPIOのまま残す→テスト順をToF最優先化で解決（修正4） |
| 07 | EEPROM | FAIL | 06の後遺症 |
| 08 | OSPI NOR | PASS | ID=C2 85 3A |
| 09 | OSPI PSRAM | FAIL | MX_OCTOSPI1_InitとBSPの競合→HAL_OSPI_DeInit(&hospi1)先行（修正6） |
| 10 | 内蔵Flash NV | PASS | Bank1 page126 |
| 11 | SRAMバッファ | PASS | |
| 12 | マイク1 ADF1 | FAIL | MX_ADF1_Initと二重初期化+msp.cがADF1クロックをHCLKに上書き→DeInit+PLL3再選択（修正5） |
| 13 | マイク2 MDF1 | FAIL | GPDMAチャネルがnon-secure属性でsecureペリフェラルを読めずゼロ→DMA_CHANNEL_SEC設定（修正5） |
| 14 | BLE STM32WB5MMG | PASS | 9600baudでAT応答あり |
| 15 | Wi-Fi EMW3080 | FAIL | NSS(PB12)未設定→HIGH出力後にリセット、タイムアウト5s（修正7） |
| 16 | TrustZone/GTZC | FAIL | SAU無効（ALLNS=1でIDAU動作はしていた）→partitionヘッダでSAU有効化（修正2） |

## 適用する修正リスト
1. ~~D:空き容量確保~~（ユーザー対応済み）
2. `partition_stm32u585xx.h`: SAU_INIT_CTRL_ENABLE=1, SAU_INIT_REGION0..3=1
3. `console.cpp`: `asm(".global _printf_float");` 追加
4. `test.cpp`: ToF(Ranging)テストをEnvセンサーより前に移動
5. `tests_audio.cpp`: HAL_MDF_DeInit(&AdfHandle0)一回実行＋Init後にADF1クロックをPLL3再設定＋Record前にHAL_DMA_ConfigChannelAttributes(SEC|SRC_SEC|DEST_SEC)
6. `tests_memory.cpp`: PSRAMテスト冒頭でHAL_OSPI_DeInit(&hospi1)
7. `tests_wireless.cpp`: Wi-Fi NSS=PB12をHIGH出力設定、タイムアウト5000ms
8. リビルド→書き込み→監視→全PASS確認

## その後の残タスク
- エッジAI推論システム（B-U585I版）簡易ファーム実装（要件定義書参照。BLE=UART4 AT server、音声分類、11.2節プロトコル、11.3節メモリマップ）
- ML→変換→転送 完全自動化パイプライン（stedgeai CLIは保留中。zip=`C:\Users\takut\Downloads\x-cube-ai-windows-v10.2.0.zip`。展開先はC:側にすること）

## 注意事項
- CubeIDE GUI起動中。GUI側ビルドとの同時実行は避ける
- ioc再生成でUSER CODE外の変更とpartition_stm32u585xx.h手編集が消える可能性
- MIC2(MDF1)はioc未設定でもBSPが自前初期化するので問題なし
- FW_U5は1.9.0（ioc表記は1.8.0だが互換動作）
