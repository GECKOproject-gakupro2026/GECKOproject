# B-U585I-IOT02A Wi-Fi STA接続手順

## 確定構成

- PC: Windowsモバイルホットスポットを提供
- 基板: EMW3080をSTAとして接続し、DHCPで `192.168.137.x` を取得
- テレメトリ: 基板上のTCPサーバー、ポート `5000`
- PCのインターネット側Wi-Fi接続先は変更しない
- EMW3080は5 GHz非対応のため、ホットスポットは必ず2.4 GHz

## 起動

PowerShellでリポジトリのルートから実行する。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_side\wifi_hotspot.ps1 -Action Start
```

既定の資格情報はFWの `CFG_WIFI_SSID` / `CFG_WIFI_PASSWORD` と一致している。変更時は両方を同じ値にしてFWを再ビルドする。

状態確認と停止:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_side\wifi_hotspot.ps1 -Action Status
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_side\wifi_hotspot.ps1 -Action Stop
```

## 基板IPの確認

基板をリセットし、ST-LINK VCPを921600 baudで開く。次のログにDHCPアドレスが出る。

```text
[TLM] Wi-Fi connected, IP=192.168.137.x
[TLM] TCP server listening on port 5000
```

ARP/近隣テーブルに基板が載っている場合は、次でもTCPサーバーを検索できる。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\pc_side\find_board.ps1
```

表示された `192.168.137.x:5000` をステータスモニターのWi-Fi (TCP)接続先、またはOTAツールの `--tcp` に指定する。

## トラブルシュート

- APがスキャン結果に出ない: `wifi_hotspot.ps1 -Action Status` の `Band` が `TwoPointFourGigahertz` か確認
- DHCPを取得できない: SSID/パスワードの一致、ホットスポットの起動状態、Windows側の接続クライアント数を確認
- IPを発見できない: UARTログを優先する。ARPテーブル検索は、PCと基板の通信履歴がない場合には検出できない
- TCP接続不可: Windows Defender Firewallと、FWログの `TCP server listening on port 5000` を確認
