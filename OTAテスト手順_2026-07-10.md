# OTAファームウェア更新 — 実機テスト手順書（次回ボード接続時）

作成: 2026-07-10（ソフト実装完了・ビルド0エラー確認済み・実機未検証）

## 現在の確定アーキテクチャ（2026-07-11）

```text
電源投入 / リセット
  -> Secure Stage-0（固定ローダー、Bank1 0x0C000000）
  -> Bank2のSP / Reset Handler / NSAP magic / versionを検査
  -> 正常かつユーザーボタン未押下ならNonSecureを自動実行
  -> 不正・未書込み・ボタン押下ならSecure Wi-Fi OTAローダーに滞在

Wi-Fi OTA
  -> Windows 2.4 GHzモバイルホットスポットへSTA接続
  -> 外部NOR 0x00200000へ候補イメージを受信
  -> NOR全読み戻しCRC検証
  -> 内蔵Bank2 0x08100000へ消去・コピー
  -> Bank2全読み戻しCRC検証
  -> NonSecureアプリを実行
```

| 領域 | 役割 |
|---|---|
| Secure Bank1 | 固定Stage-0、TrustZone設定、Wi-Fi OTA、検証、復旧 |
| NonSecure Bank2 | OTA更新される通常アプリ（現在v9、緑LED点滅） |
| 外部NOR 64MB | OTAステージング。候補スロットA(0x200000)+直前正常版スロットB(0x400000)のA/B構成 |
| 外部PSRAM 8MB | 将来のAI activations、音声、センサー、OTAワークバッファ |

復旧操作: **ユーザーボタンを押しながらリセット**すると、正常なNonSecureイメージがあってもSecure OTAローダーに留まる。

BootGuard（Phase 2、TAMP->BKP0Rの起動試行カウンタ、電源保持ドメインなのでウォームリセットでは消えない）: NonSecureは起動が主ループへ到達すると`Secure_ConfirmBoot()`（NSCゲートウェイ）でカウンタを0クリアする。クリアされないまま`kMaxAttempts=3`回リセットが続くと、次の起動時にStage-0がNORスロットBの直前正常版をBank2へ自動復元する。

## 実施履歴の要約

- センサー16項目、UART/TCPテレメトリ、BLE、音声、AI推論を実装・実機検証
- EMW3080を基板SoftAPからWindows 2.4 GHzホットスポット接続のSTA方式へ変更
- UART/TCP共通OTAフレーム、再送、CRC、外部NORステージングを実装
- `FW_APPLY (0x08)`でNonSecure Bank2への適用と実行を実装
- TrustZone GPIO属性を修正し、NonSecureから赤/緑LEDの両方を制御可能にした
- NonSecure v2〜v5をWi-Fi OTAし、現在はv5（赤消灯、緑250ms点滅）
- Secure Stage-0にBank2検査、自動起動、ボタンによる復旧モードを追加
- 音声テストPCMを4096から2048サンプルへ削減し、Secure RAMを4096B削減

## 今後の実装計画

### Phase 1: 固定ローダーと自動起動（完了）

- [x] Secure Stage-0をBank1に固定
- [x] NonSecureイメージの基本検査
- [x] 正常時の自動起動
- [x] ボタン押下時のOTA復旧モード
- [x] リセット後の自動起動を実機確認

### Phase 2: 外部NOR A/Bロールバック（完了・実機検証済み 2026-07-12）

- [x] NORメタデータを2コピー化（`ota::SlotMeta`: magic/size/crc16/version/selfCrc、primary+mirrorをスロットB末尾0x1F0000に配置）
- [x] `0x00200000-0x003FFFFF`を候補スロットAとして継続使用
- [x] `0x00400000-0x005FFFFF`を直前正常版スロットBに追加
- [x] Bank2更新前に現行正常版をBへ退避（`Manager::backupCurrentBank2()`、`FW_APPLY`実行時に自動）
- [x] 起動成功フラグ未確定時はBからBank2を復元（BootGuard、TAMP->BKP0Rカウンタ、`kMaxAttempts=3`）
- [x] 実機試験: 起動未確認FW(v8)を4回リセット→自動ロールバックでBank2がv7に復元（SWDで`0x08100404`のversionフィールドを確認）
- [x] 実機試験: 正常FW(v9)を4回通常リセット→誤ロールバックなし（versionはv9のまま維持）
- [ ] OTA中断・電源断中のバックアップ書き込み欠落など、より過酷な異常系は未試験

### Phase 3: セキュリティ強化

- [ ] CRCに加えて公開鍵署名を検証
- [ ] 単調増加versionによるダウングレード防止
- [ ] 鍵・更新ポリシーをSecure領域に限定

### Phase 4: 外部PSRAM活用

- [ ] まずOTA検証・ログの非DMAワーク領域で動作確認
- [ ] キャッシュ、TrustZone、メモリマップを検証
- [ ] AI activations、音声リング、センサーバッファを段階的に移動

## 今回実装した内容（ソフトのみ、実機テスト待ち）

| 部位 | ファイル | 内容 |
|---|---|---|
| FW | `Secure/Core/Inc(Src)/frame_codec.h/.c` | 受信ストリーミングデコーダ追加＋OTAコマンドID（0x04 FW_CHUNK / 0x05 FW_COMPLETE / 0x06 STATUS_REQ / 0x07 STATUS_RESP / 0x7E ACK / 0x7F NACK、11.2節準拠） |
| FW | `Secure/Core/Inc(Src)/ota.hpp/.cpp` | ステージング管理: 外部NOR FW領域B(0x200000, 最大2MB, 11.3節マップ)へ64KBブロック単位のオンデマンド消去＋逐次書込→FW_COMPLETEで全読み戻しCRC16検証 |
| FW | `telemetry.cpp/.hpp` | UART/TCP両リンクの受信バイトをフレームデコーダへ（1文字コマンド a/s/t/p/l は従来どおり共存）。ACK/NACK/STATUS_RESP応答 |
| FW | `app_main.cpp` | コンソール受信をprocessRxByte経由に |
| PC | `pc_side/status_monitor/ota_update.py` | 送信ツール: チャンク1008B＋ACK待ち(1.5s)＋リトライ3回、完了時CRC検証(30s待ち)、`--status`照会 |
| PC | `protocol.py` | `build_frame()`エンコーダ＋OTA定数（ラウンドトリップ検証済み） |

再送・重複対策: チャンクは逐次オフセット必須。**直前チャンクの再送（ACK喪失時）は再ACKのみで二重書き込みしない**。offset=0で常に転送やり直し可能。

## 書き込み事項（ボード接続後、最初にやること）

```bash
# 1. ビルド（済み。再ビルドする場合）
HWS="<scratchpad>/hws"
"d:/App/ST/STM32CubeIDE_2.1.1/STM32CubeIDE/stm32cubeidec.exe" --launcher.suppressErrors -nosplash \
  -application org.eclipse.cdt.managedbuilder.core.headlessbuild -data "$HWS" -build "B-U585I-IOT02A_Secure/Debug"

# 2. 書き込み（オプションバイト変更は不要。NS側・Bank2はそのまま）
CLI="d:/App/ST/STM32CubeIDE_2.1.1/STM32CubeIDE/plugins/com.st.stm32cube.ide.mcu.externaltools.cubeprogrammer.win32_2.2.400.202601091506/tools/bin/STM32_Programmer_CLI.exe"
"$CLI" -c port=SWD mode=UR -d "Secure/Debug/B-U585I-IOT02A_Secure.elf" -v
"$CLI" -c port=SWD mode=HOTPLUG -rst
```

前提条件（変更なしのはず、崩れていたら要確認）:
- TZEN=1 / SECWM1=Bank1全Secure / SECWM2=Bank2全NonSecure / RDP=0xAA
- COM9 / 921600baud
- EMW3080=V2.3.4、Windowsモバイルホットスポット（`pc_side/wifi_hotspot.ps1`で起動。既定SSID=`U585-IOT02A`/pass=`u585iot02a`、2.4 GHz固定。**このスクリプトはPCのホットスポット設定を上書きする**ので実行前のSSIDが変わる点に注意）、基板はSTA接続

## テスト用イメージの作成

任意のバイナリで良いが、実運用と同じものとして現行FWのbinを使う:

```bash
"<gcc-plugin>/tools/bin/arm-none-eabi-objcopy.exe" -O binary \
  "Secure/Debug/B-U585I-IOT02A_Secure.elf" "<scratchpad>/ota_test.bin"
# 約220KB。まず小さいファイル(数KB)で疎通→本物サイズの順が安全
```

## テスト手順

### T1. UART経由のOTAステージング（基本）
1. 起動ログ確認（`[TLM] streaming:`まで出ること）
2. 音声ストリーミングOFFであること（デフォルトOFF。ONにしていたら 's' 送信）
3. `cd pc_side/status_monitor && python ota_update.py <scratchpad>/ota_test.bin --port COM9`
4. **期待**: 進捗表示→ボードログ`[OTA] receiving new image...`→完了時`[OTA] image staged: NNN bytes, crc=0xXXXX (NOR 0x00200000)`→PC側`OK: image staged and CRC-verified`→state=Staged
5. 転送レート記録（見込み: 実効30-60KB/s、初回は64KB毎に~0.4sの消去停頓あり）

### T2. ステータス照会
```bash
python ota_update.py --status --port COM9
# state=Staged / received=サイズ / image crc16 が T1 と一致すること
```

### T3. Wi-Fi(TCP)経由のOTA
1. `pc_side/wifi_hotspot.ps1 -Action Start` でPCのモバイルホットスポットを起動（既定SSID=`U585-IOT02A`、2.4 GHz固定）
2. UARTログで基板のDHCPアドレスを確認
3. `python ota_update.py <scratchpad>/ota_test.bin --tcp 192.168.137.x:5000`
3. 期待はT1と同じ。**注意**: TCPリンクではテレメトリ30Hzが流れ続ける中でOTAが混流する（ツールは無関係フレームをスキップする設計）

### T4. 異常系
| ケース | 操作 | 期待 |
|---|---|---|
| 転送中断→再開 | 転送中にCtrl+C→再度実行 | offset=0から自動やり直しで成功 |
| ACK喪失の再送 | （自動: リトライログが出たら観察） | 重複チャンクを再ACKし整合が崩れない |
| CRC不一致 | binの1バイトを書き換えたコピーを送る…はできないので、`--status`直後に完了だけ再送する等 | NACK(VERIFY or BAD_STATE)で停止し、ボードは動作継続 |
| テレメトリ共存 | T1中にUIアプリ（別COMは不可・UART排他）→TCP側で接続して確認 | OTA中もステータス配信が継続 |

### T5. 回帰確認
- OTA後に通常機能が無事なこと: 全データタブ更新継続、音声 'a'→ストリーミング、（可能なら）'t'で16項目テスト

## 検証ポイントまとめ（2026-07-11 実機テスト完了）
- [x] T1: UARTステージング成功 — 1,040,416B / 25.5s / **39.9KB/s** / CRC 0xDCC9 一致
- [x] T2: --statusでStaged確認（Staged / 1040416B / 0xDCC9）
- [x] T3: TCPステージング成功 — 64KB / 9.9s / 6.5KB/s / CRC 0x35BC 一致（SoftAP経由）
- [x] T4: リトライ/再ACK動作を転送中に自然発生で確認（timeout→retry→継続成功）
- [x] T5: 回帰OK — テレメトリ50.3Hz/CRC0、音声3.97s欠損ほぼ0、16項目テスト起動可
- [x] 転送実効レート: UART 39.9KB/s、Wi-Fi TCP 6.5KB/s（テレメトリ混流あり）
- [x] T6: AI推論 — `[AI] silence 52%/0%/48% (10ms)`、モデルロード254,864 MACC

## STA方式での再検証（2026-07-11）

- [x] STA版Secure FWをビルド、SWD書き込み、ベリファイ成功
- [x] Windowsモバイルホットスポットを2.4 GHzで起動（PCのインターネット側Wi-Fiは維持）
- [x] EMW3080 V2.3.4がSTA接続、DHCP `192.168.137.185` を取得
- [x] TCPサーバー `192.168.137.185:5000` 起動確認
- [x] TCP OTAステージング成功 — 1,040,416B / 101.6s / 10.0KB/s / CRC 0xF62B一致
- [x] 別接続の `--status` で `Staged / 1040416B / 0xF62B / last errorなし` を確認
- [x] 初回ACK timeout後のretryで継続成功（重複チャンク再ACK経路）

## 全フロー回帰テスト（2026-07-12）

v5自動起動状態からユーザーボタンを押しながらリセットしてSecure OTAローダーへ復帰し、T1〜T6を通しで再確認。

- [x] Secure/NonSecure再ビルド0エラー確認後、Secure ELFをbin化してテスト画像に使用（1,040,416B, crc16=0xE35F）
- [x] T1: UARTステージング成功 — CRC 0xE35F一致、state=Staged
- [x] T2: --statusでStaged確認（received=staged=1,040,416B、crc一致）
- [x] T3: Wi-Fi TCPステージング成功 — ボードIP `192.168.137.112:5000`（`find_board.ps1`のARP探索で発見）、CRC 0xE35F一致
- [x] T4: UART転送を意図的に中断（timeout 3s）→再実行でoffset=0から自動やり直し、24.4s/41.6KB/sで`OK: image staged and CRC-verified`
- [x] T5: OTA後もSecure UARTテレメトリ・OTAステータス照会が正常応答（回帰OK）
- [x] T6: `i`コマンドで単発AI推論 — `[AI] silence silence=85% tone=0% noise=15% (10ms)`、モデルロード254,864 MACC
- [x] ボタン長押しリセットによるOTA復旧モードへの手動遷移を実運用で確認（v5自動起動中からの復帰）

## Wi-Fi OTA適用・実行テスト（2026-07-11）

- [x] Secureローダーに `FW_APPLY (0x08)` を追加
- [x] 適用対象をNonSecure Bank2 (`0x08100000`) に限定し、SP/Resetベクタを事前検証
- [x] NonSecure v2をビルド（4,569B、赤LED消灯・緑LED常時点灯）
- [x] Wi-Fi TCPで転送、NORステージングCRC `0x04DF` 一致
- [x] Bank2消去・書込み・内蔵Flash CRC再検証成功
- [x] NonSecure v2へジャンプして実行（適用後UARTテレメトリ停止、LED表示切替）
- [x] Bank2ベクタ確認: SP=`0x200C0000`、Reset=`0x0810058D`

実行コマンド:

```powershell
python pc_side\status_monitor\ota_update.py NonSecure_LED_v2.bin `
  --tcp 192.168.137.185:5000 --apply
```

### 点滅版v3への再更新

- [x] NonSecure v3をビルド（4,617B、赤LED消灯・緑LEDを250msごとに反転）
- [x] Wi-Fi TCPで転送、NOR CRC `0xD089` 一致
- [x] Bank2適用・内蔵Flash再検証・NonSecure起動成功
- [x] Bank2アプリ情報をSWDで確認: magic=`0x4E534150 (NSAP)`、version=`3`
- [x] 適用後のSecure UART出力0 bytes/秒を確認（NonSecureへ制御移行）

### LED/自動起動版v4〜v5

- [x] TrustZoneのGPIO属性でPH6/PH7をNonSecureへ明示開放
- [x] v4で赤/緑の交互点灯を確認
- [x] v5をWi-Fi OTA（4,617B、CRC 0xC5CD）
- [x] v5で赤消灯・緑250ms点滅を確認
- [x] Secure Stage-0更新後、通常リセットでv5を自動起動
- [x] 自動起動後のSecure UART出力0 bytes/秒を確認

### 実機テストで発覚し修正した2件（重要）
1. **UART RXの取りこぼし**: `HAL_UART_Receive`ポーリング(timeout=0)は瞬間のRXNEしか見えず、50Hzテレメトリ下ではPCからの受信バイトをほぼ全て取りこぼしていた → **GPDMA1_CH1のリンクリスト循環DMA受信**に変更（console.cpp）。TrustZoneでは**ノードのSrcSecure/DestSecureに加えチャネル自体のSEC|PRIV属性が必須**（無いとULEで黙って停止）
2. **PC側の受信バックログ**: Windows USB-CDCは`reset_input_buffer()`1回では掃けない → ota_update.pyの接続時に「静穏0.3sまでドレイン」+`in_waiting`一括読みを追加

## 既知の制約・次フェーズ
- 外部NORから内蔵Bank2への適用とNonSecure実行は実装済み
- 起動時検査は現在SP、Reset Handler、NSAP magic、version。起動時の署名検証は未実装（Phase 3）
- NORは候補スロットA(0x200000)+直前正常版スロットB(0x400000)のA/B構成、BootGuardによる自動ロールバックまで実装・実機検証済み（Phase 2完了）
- Secureローダーを強制起動するにはユーザーボタンを押しながらリセットする
- NORテスト（テスト08）は0x03FF0000（最終ブロック）を使うためステージング領域0x200000-0x3FFFFFとは干渉しない
- OTA中の音声ストリーミングは帯域を食い合うためOFF推奨（ONでも動くはずだがレート低下）
- BootGuardの電源完全断（バッテリー/USB両方切断）時の挙動は未検証。TAMP->BKP0RはVBATドメインなのでUSB給電継続中のリセットでは保持されるが、完全電源断でクリアされる可能性がある

## OTA Phase 2実機テスト（2026-07-12）: NOR A/Bロールバック

新規実装: `Secure/Core/Inc(Src)/boot_guard.hpp/.cpp`（TAMP->BKP0R起動試行カウンタ）、`ota.hpp/.cpp`にスロットB退避/復元（`backupCurrentBank2()`/`restoreFromBackup()`/`hasValidBackup()`）、`secure_nsc.h/.c`に`Secure_ConfirmBoot()` NSCゲートウェイ追加、`main.c`(Secure)にロールバック判定、NonSecure `main.c`起動完了時に`Secure_ConfirmBoot()`呼び出しを追加。

- [x] Secure/NonSecure再ビルド0エラー確認、Secure/NonSecureともボードへ書込み
- [x] v6自動起動・LED点滅を確認（BootGuard組み込み後も既存フローに影響なし）
- [x] Wi-Fi TCP経由でv7を`--apply`適用 → UARTログで`[OTA] Bank2 backup complete, crc=0xF7AD`を確認（v6がスロットBへ退避）
- [x] 意図的に`Secure_ConfirmBoot()`を呼ばないv8をビルドし同様に適用
- [x] v8適用後、通常リセット（`STM32_Programmer_CLI -rst`）を4回実行
- [x] 4回目のリセットでUART/OTA応答が停止（Bank2復元中またはNonSecure実行中と判定）、SWDで`0x08100404`（NSAP version）を読み取りv7=`0x00000007`を確認 → **自動ロールバック成功**
- [x] `Secure_ConfirmBoot()`を復元したv9をビルド・適用（version=9をSWDで確認）
- [x] v9に対し通常リセットを4回実行 → versionはv9のまま維持、誤ロールバックなしを確認
- [x] 以降、通常リセットは`STM32_Programmer_CLI.exe -c port=SWD mode=HOTPLUG -rst`でコマンド実行可能（ユーザーボタン操作が必要なのはSecure OTAローダーへの強制復帰のみ）

---

# 追記（2026-07-11）: AI推論の実機テスト（T6）

AIパイプライン導入済み（`pc_side/ml_pipeline/`、詳細は同フォルダREADME）。ボード接続時に追加で:

### T6. AI推論
1. 最新FW書き込み後、COM9で `i` を送信 → `[AI] <クラス名> silence=..% tone=..% noise=..% (Nms)` が出ること
2. `I` で自動推論ON → 2秒毎に結果が流れること。静音時=silence、口笛や音楽=tone、ざわめき=noise に反応するか確認
3. 推論時間の記録（見込み: 数ms〜数十ms @160MHz）
4. パイプライン一気通し: `cd pc_side/ml_pipeline && python pipeline.py --skip-train`（ST-LINK書き込みまで全自動）
5. OTA経由: `python pipeline.py --skip-train --ota COM9`（※適用は次フェーズ、ステージングまで）

- [ ] T6-1: 'i'で推論結果表示
- [ ] T6-2: クラス反応確認（silence/tone/noise）
- [ ] T6-3: pipeline.py全自動実行成功
